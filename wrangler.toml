name = "audiar-worker"
main = "index.ts"
compatibility_date = "2025-01-01"

# Secrets (never commit real values):
#   wrangler secret put FREESOUND_API_KEY
#   wrangler secret put STABILITY_API_KEY   # once stableAudio.ts is wired up
#   wrangler secret put GROQ_API_KEY        # once groqTTS.ts is wired up
#
# For local dev, copy .dev.vars.example to .dev.vars and fill in real keys —
# `wrangler dev` reads .dev.vars automatically and it's gitignored.
